// import { utilActions } from "../constants/utilActionTypes"

const initialState = {
    
}

export const utilReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state
    }
}